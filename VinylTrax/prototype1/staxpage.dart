import 'package:flutter/material.dart';

class StaxPage extends StatelessWidget {
  const StaxPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Stax of Trax Page'),
    );
  }
}
